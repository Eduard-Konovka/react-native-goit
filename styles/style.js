import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  main: {
    flex: 1,
  },
  bgImage: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
